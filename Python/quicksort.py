"""
QuickSOrt
"""
def quicksort(array):
    leftpiv = array[0]
    mid = len(array) // 2
    rightpiv = array[-1]

def partitition(leftpiv,mid,rightpiv,array):


    if leftpiv > array[j]:
        rightside.append(array[j])
    else:
        leftside.append(array[j])
    if rightpiv > array[j]:
        rightside.append(array[k])
    else:
        rightside.append(array[k])
